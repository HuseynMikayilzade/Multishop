@model PaginationVm<Color>

@{
    int count = 0;
}
<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Color</h4>
                <div class=" d-flex justify-content-between">
                    <p class="card-description">
                        <a asp-action="create" asp-controller="color"><button type="button" class="btn btn-success">Create color</button> </a>
                    </p>
                    <nav aria-label="" style="margin-right:50px;">
                        <ul class="pagination">
                            <li class="page-item @(Model.CurrentPage<=1?"disabled":"")">
                                <a class="page-link" asp-route-page="@(Model.CurrentPage-1)" tabindex="-1">Previous</a>
                            </li>

                            @for (int i = 0; i < Model.TotalPage; i++)
                            {
                                <li class="page-item @(Model.CurrentPage-1==i ? "btn-success  active" : "")">
                                    <a class="page-link" asp-route-page="@(i+1)" asp-action="index" asp-controller="color">@(i + 1)</a>
                                </li>
                            }

                            <li class="page-item @(Model.CurrentPage>=Model.TotalPage?"disabled":"")">
                                <a class="page-link" asp-route-page="@(Model.CurrentPage+1)">Next</a>
                            </li>
                        </ul>
                    </nav>
                </div>
                <div class="table-responsive pt-3">
                    <table class="table table-dark text-center">
                        <thead>
                            <tr>
                                <th>
                                    No
                                </th>

                                <th>
                                    Name
                                </th>

                                <th>
                                    Settings
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Items)
                            {
                                count++;
                                <tr>
                                    <td>
                                        @count
                                    </td>

                                    <td>
                                        @item.Name
                                    </td>

                                    <td align="center">
                                        <a asp-controller="color" asp-action="update" asp-route-id="@item.Id" type="button" class="btn btn-outline-warning btn-fw" alt="update"><i class="mdi mdi-grease-pencil"></i></a>
                                        <a asp-controller="color" asp-action="delete" asp-route-id="@item.Id" type="button" class="btn btn-outline-danger btn-fw" alt="delete"><i class="mdi mdi-delete-sweep"></i></a>
                                        <a asp-controller="color" asp-action="detail" asp-route-id="@item.Id" type="button" class="btn btn-outline-primary btn-fw" alt="more"><i class="mdi mdi-more"></i> </a>
                                    </td>
                                </tr>
                            }

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>